console.log("I made a follow request")


var existingFollowRequests = $(".<%= dom_id(@follow_request.sender) %>_<%= dom_id(@follow_request.recipient)%>_follow_unfollow");

var updatedFollowRequests = $("<%= j(render "follow_requests/follow_unfollow", sender: @follow_request.sender, recipient: @follow_request.recipient)%>");

existingFollowRequests.replaceWith(updatedFollowRequests);
